version: "3.9"

services:
  postgres:
    image: postgres:17
    container_name: postgres_spring
    restart: always
    environment:
      POSTGRES_DB: persocloud
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5440:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    container_name: spring_app
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/persocloud
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      JWT_SECRET_KEY: "E4xEHxWa6AUHmbfvQxQiBM1oECP07drQ7/WdIyCWgxE="
      STORAGE_IMAGES: "/var/certificados"
    depends_on:
      - postgres
    volumes:
      - /var/certificados:/var/certificados

volumes:
  postgres_data:
